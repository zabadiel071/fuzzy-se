package inference;

import files.FileController;

public class MasterFam extends FileController{

    /**
     * Constructor por defecto
     *
     * @param fileName : Nombre del archivo en la estructura de archivos
     */
    protected MasterFam(String fileName) {
        super(fileName);
    }
}
